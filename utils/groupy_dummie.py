"""
Dummie functions to import when running without installed GrouPy package
"""


def P4ConvZ2(in_channels,
             out_channels,
             kernel_size,
             stride=1,
             padding=1,
             bias=True):
    return None


def P4ConvP4(in_channels,
             out_channels,
             kernel_size,
             stride=1,
             padding=1,
             bias=True):
    return None
